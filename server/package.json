{
  "name": "server_asset",
  "version": "1.0.0",
  "description": "server asset",
  "license": "ISC",
  "author": "",
  "type": "typescript",
  "main": "index.ts",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:clear": "jest --clearCache",
    "test:verbose": "jest --verbose",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "node dist/index.js",
    "dev": "nodemon --exec ts-node src/index.ts",
    "cli": "ts-node --transpile-only src/cli/cli-runner.ts",
    "cli:help": "npm run -s cli help",
    "cli:version": "npm run -s cli version",
    "cli:list": "npm run -s cli list",
    "db": "npm run -s cli db",
    "db:help": "npm run -s cli db help",
    "env": "npm run -s cli env",
    "env:help": "npm run -s cli env help",
    "env:list": "npm run -s cli env list",
    "env:get": "npm run -s cli env get",
    "env:set": "npm run -s cli env set",
    "env:create": "npm run -s cli env create",
    "env:validate": "npm run -s cli env validate"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/express": "^5.0.3",
    "@types/jest": "^30.0.0",
    "@types/morgan": "^1.9.10",
    "@types/multer": "^2.0.0",
    "@types/node": "^24.0.4",
    "@types/supertest": "^6.0.3",
    "express": "^5.1.0",
    "jest": "^30.0.3",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.1",
    "ts-jest": "^29.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.19",
    "@types/jsonwebtoken": "^9.0.10",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "cli-table3": "^0.6.5",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "multer": "^2.0.2",
    "pg": "^8.16.2",
    "pg-pool": "^3.10.1",
    "reflect-metadata": "^0.2.2",
    "typeorm": "^0.3.25",
    "uuid": "^11.1.0"
  }
}
